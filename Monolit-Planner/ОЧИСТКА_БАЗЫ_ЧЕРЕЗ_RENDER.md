# 🔴 ОЧИСТКА БАЗЫ ДАННЫХ ЧЕРЕЗ RENDER DASHBOARD

## Метод 1: Через веб-интерфейс Render (САМЫЙ ПРОСТОЙ)

### Шаг 1: Откройте PostgreSQL в Render

1. Зайдите на https://dashboard.render.com
2. Войдите в свой аккаунт
3. Найдите в списке сервисов **PostgreSQL** базу данных `monolit_planner`
4. Кликните на неё

### Шаг 2: Откройте Shell доступ к базе

1. В верхней части страницы найдите кнопку **"Connect"** (Подключиться)
2. В выпадающем меню выберите **"PSQL Command"**
3. Скопируйте команду (она будет выглядеть так):
   ```
   PGPASSWORD=XG78v4ASVxwe3X8uEg0Cma6tviE7xcVx psql -h dpg-d4ao5tripnbc73aegphg-a.oregon-postgres.render.com -U monolit_user monolit_planner
   ```

### Шаг 3: Если у вас НЕТ терминала

**Вариант A: Используйте Render Shell (рекомендуется)**

1. В меню сервиса PostgreSQL найдите вкладку **"Shell"**
2. Нажмите кнопку **"Launch Shell"**
3. Откроется веб-терминал прямо в браузере!
4. Скопируйте и вставьте эти команды (всё сразу):

```sql
-- 1. Проверьте текущее состояние
SELECT COUNT(*) as projects FROM monolith_projects;
SELECT COUNT(*) as bridges FROM bridges;
SELECT COUNT(*) as positions FROM positions;

-- 2. УДАЛИТЬ ВСЁ (скопируйте всё это вместе)
BEGIN;
DELETE FROM positions;
DELETE FROM parts;
DELETE FROM snapshots;
DELETE FROM bridges;
DELETE FROM monolith_projects;
COMMIT;

-- 3. Проверьте результат
SELECT COUNT(*) as projects FROM monolith_projects;
SELECT COUNT(*) as bridges FROM bridges;
SELECT COUNT(*) as positions FROM positions;
```

**Ожидаемый результат:**
```
projects: 0
bridges: 0
positions: 0
```

---

## Метод 2: Попросить кого-то выполнить SQL команду

Если у вас нет доступа к Render Dashboard, отправьте эту команду тому, у кого есть доступ:

```sql
BEGIN;
DELETE FROM positions;
DELETE FROM parts;
DELETE FROM snapshots;
DELETE FROM bridges;
DELETE FROM monolith_projects;
COMMIT;
```

---

## Метод 3: Подождать автоматического деплоя

После того как бэкенд задеплоится с исправленной схемой:

1. База данных автоматически инициализируется с правильной схемой
2. Старые ошибочные данные будут игнорироваться
3. Новые импорты Excel будут работать правильно

**НО:** Старые данные останутся в БД (мусор). Лучше очистить.

---

## ❗ Что делать если ничего не работает

### Проверьте статус деплоя бэкенда

1. Откройте https://dashboard.render.com
2. Найдите сервис **"Monolit-Planner API"** (backend)
3. Проверьте статус:
   - 🟢 **Live** - деплой завершён
   - 🔵 **Deploying** - идёт деплой (подождите)
   - 🔴 **Failed** - ошибка (проверьте логи)

### Проверьте логи бэкенда

1. Откройте сервис **"Monolit-Planner API"**
2. Перейдите на вкладку **"Logs"**
3. Ищите строки:
   ```
   ✅ [PostgreSQL] Schema initialized successfully
   ✅ [PostgreSQL] Running 15 CREATE TABLE statements...
   ✅ [PostgreSQL] ✓ Created table
   ```

### Если видите ошибки в логах

Скопируйте последние 50 строк логов и покажите мне. Я помогу разобраться!

---

## ✅ После очистки базы данных

### Проверьте что всё работает:

1. **Откройте Monolit Planner**: https://monolit-planner-frontend.onrender.com
2. **Список проектов должен быть ПУСТОЙ** (это нормально!)
3. **Импортируйте Excel файл** (тот же, что не работал раньше)
4. **Проекты должны появиться** ✅
5. **НАЖМИТЕ F5** (обновить страницу)
6. **ПРОЕКТЫ ДОЛЖНЫ ОСТАТЬСЯ!** ← Это и есть исправление! ✅

---

## 📞 Если проблемы остались

Покажите мне:

1. **Скриншот** Render Dashboard (страница базы данных)
2. **Логи бэкенда** (последние 50 строк)
3. **Скриншот** браузерной консоли (F12 → Console)

Я помогу!

---

**Важно:** После очистки базы данных все старые данные будут удалены. Если у вас есть важные проекты - сделайте бэкап перед очисткой!
