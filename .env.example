# ⚠️ IMPORTANT: This is a template file. Never commit real API keys!
# Copy this file to .env and fill in your actual values locally only.
# Add .env to .gitignore (it already should be)

# ========== LLM PROVIDER CONFIGURATION ==========
# SECURITY: Set LLM_PROVIDER to one of: claude, openai, perplexity
LLM_PROVIDER=claude

# Claude API Key
# Get key from: https://console.anthropic.com/account/keys
# Format: starts with sk-ant-
# ⚠️ NEVER commit this value - use environment variables in production
LLM_API_KEY=REPLACE_WITH_YOUR_CLAUDE_API_KEY

# Claude model selection
LLM_MODEL=claude-sonnet-4-6
LLM_TIMEOUT_MS=30000

# OpenAI Configuration (optional)
# Get key from: https://platform.openai.com/account/api-keys
# Format: starts with sk-proj-
# Only needed if LLM_PROVIDER=openai
# OPENAI_API_KEY=REPLACE_WITH_YOUR_OPENAI_API_KEY

# Perplexity Configuration (optional)
# Get key from: https://www.perplexity.ai/settings/keys
# Only needed if LLM_PROVIDER=perplexity
# PPLX_API_KEY=REPLACE_WITH_YOUR_PPLX_API_KEY
# PPLX_MODEL=sonar
# PPLX_TIMEOUT_MS=60000

# ========== SERVER CONFIGURATION ==========
NODE_ENV=development
PORT=3001

# ========== CORS CONFIGURATION ==========
# SECURITY: Only specify trusted origins in production!
# For local development, use http://localhost:3000
# For production, specify exact origins: https://yourdomain.com
# NEVER use * in production - it allows any origin to access your API
CORS_ORIGIN=http://localhost:3000

# For multiple origins, use comma-separated list:
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ========== DATABASE CONFIGURATION (if needed) ==========
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=stavagent
# DB_USER=postgres
# DB_PASSWORD=REPLACE_WITH_YOUR_DB_PASSWORD

# ========== LOGGING CONFIGURATION ==========
# Log level: error, warn, info, debug
LOG_LEVEL=info

# ========== SECURITY HEADERS ==========
# Enable/disable security headers (HSTS, CSP, etc.)
ENABLE_SECURITY_HEADERS=true
