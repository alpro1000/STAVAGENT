# URS Matcher Service — Готовый набор документов для разработки

## Что это?

Полный комплект документации (ТЗ + промпты) для разработки web-сервиса подбора позиций **ÚRS** (чешский каталог строительных работ и услуг) на основе описаний работ (**výkaz výměr**).

Сервис:
- Принимает Excel/ODS/CSV файлы или текстовые описания работ
- Подбирает позиции из каталога ÚRS с помощью ИИ и онлайнового поиска
- Генерирует сопутствующие работы (tech-rules)
- Возвращает структурированный результат (JSON + таблица)

---

## 📋 Файлы в комплекте

### 1. **URS_MATCHER_TZ.md** — Техническое задание v1.0

Полное описание:
- Цель и задачи сервиса
- Основные сценарии использования (use-cases)
- Входные и выходные форматы данных
- Архитектура компонентов
- Логика обработки (pipeline)
- Интеграция с LLM (OpenAI/Claude) и Perplexity
- Нефункциональные требования
- План по этапам (MVP-1 → MVP-2 → MVP-3 → Production)
- API-endpoints
- Критерии приёма
- Структура файлов для разработчика

**Используй:** Для понимания требований, планирования разработки, согласования с командой.

---

### 2. **URS_MATCHER_DEVELOPER_PROMPT.md** — Готовый промпт для разработчика

Система инструкций для Claude / ChatGPT / OpenAI для автоматического проектирования backend:

- System Prompt для AI (копируй в Playground)
- Требования к архитектуре
- Требования к OpenAPI-спецификации
- Требования к БД-схеме
- Требования к исходному коду (какие файлы, сколько строк, что в каждом)
- Примеры структуры кода
- Критерии качества

**Используй:** Скопируй текст из раздела "SYSTEM PROMPT для Codex / Claude / ChatGPT" в Playground Claude/ChatGPT, и AI спроектирует архитектуру + напишет код.

---

### 3. **URS_MATCHER_README.md** — Этот файл

Навигация по комплекту документов.

---

## 🚀 Как начать разработку?

### Вариант A: Использовать AI для проектирования

1. **Открой Claude / ChatGPT / OpenAI Playground**
2. **Вставь в System Prompt текст из `URS_MATCHER_DEVELOPER_PROMPT.md`** (раздел "SYSTEM PROMPT для Codex...")
3. **В User Message вставь полный текст ТЗ из `URS_MATCHER_TZ.md`**
4. **Запроси:** "Спроектируй backend-сервис согласно этому ТЗ и выдай код"
5. **AI выдаст:**
   - Архитектуру
   - OpenAPI-спецификацию (YAML)
   - Схему БД (SQL)
   - Исходный код всех основных модулей
   - README с инструкциями

### Вариант B: Ручная разработка

1. Прочитай полное ТЗ (`URS_MATCHER_TZ.md`)
2. Спроектируй архитектуру (комп-то, стек)
3. Определи API-endpoints (OpenAPI)
4. Разработай схему БД
5. Напиши код модульно (сначала парсер, потом matcher, потом LLM/Perplexity)

---

## 📖 Структура ТЗ

### Быстрое ознакомление (30 мин)

Прочитай разделы:
- 1. Цель сервиса
- 2. Основные сценарии
- 5. Архитектура (5.1 — Компоненты)

### Полное ознакомление (2–3 часа)

Прочитай всё ТЗ целиком, обрати внимание на:
- Раздел 4 — Выходной формат (JSON структура)
- Раздел 6 — Логика обработки (pipeline)
- Раздел 10 — План по этапам (что в каком MVP)
- Раздел 11 — API-endpoints

---

## 🎯 Основные принципы разработки

1. **MVP-first approach:**
   - Сначала простая версия без LLM и Perplexity (MVP-1 — 2–3 недели)
   - Потом добавляй LLM и tech-rules (MVP-2 — 2–3 недели)
   - Потом Perplexity и веб-UI (MVP-3 — 2–3 недели)

2. **Не придумывай коды ÚRS:**
   - Выбирай ТОЛЬКО из каталога ÚRS
   - Никогда не генерируй несуществующие коды
   - Если нет уверенности — верни несколько кандидатов с confidence

3. **Технологические правила (tech-rules):**
   - Если есть бетонная деска → нужны bednění (опалубка) + výztuž (армирование)
   - Если есть работы в земле → нужны výkopy (земляные) + zásypy (засыпка)
   - Каждая дополнительная позиция должна быть отмечена `extra_generated = true`

4. **Прозрачность:**
   - Пользователь должен видеть, как и почему выбран каждый код
   - Сохраняй список кандидатов, причины исключения, применённые правила

---

## 🔧 Технологический стек (рекомендации)

### Backend (MVP-1)

- **Язык:** Node.js (рекомендуется для быстрого старта)
- **Фреймворк:** Express.js
- **БД:** PostgreSQL (production) / SQLite (для разработки/MVP)
- **Парсер Excel:** `xlsx` или `exceljs`
- **ODS:** `odsjs` или `zip` + XML-парс
- **Логирование:** `winston` или `bunyan`
- **Валидация:** `joi` или `zod`

### Testing

- **Unit-тесты:** Jest или Mocha
- **API-тесты:** Supertest

### Documentation

- **OpenAPI:** Swagger/OpenAPI 3.0 (YAML)
- **Swagger UI:** `swagger-ui-express`

### MVP-2 (LLM интеграция)

- **OpenAI SDK:** `openai` (npm)
- **Claude SDK:** `@anthropic-ai/sdk` (npm)
- **Embeddings:** OpenAI embeddings API

### MVP-3 (Perplexity + Frontend)

- **Perplexity API:** REST или SDK
- **Frontend:** React / Vue / Angular (на выбор)

---

## 📊 Примеры использования API

### Пример 1: Загрузить Excel-файл

```bash
curl -X POST http://localhost:3001/api/jobs/file-upload \
  -F "file=@my_budget.xlsx" \
  -F "object_type=bridge"
```

**Ответ (201 Created):**
```json
{
  "job_id": "uuid-1234-5678",
  "status": "processing",
  "message": "File uploaded, processing started"
}
```

### Пример 2: Получить результаты

```bash
curl http://localhost:3001/api/jobs/uuid-1234-5678
```

**Ответ (200 OK):**
```json
{
  "job_id": "uuid-1234-5678",
  "status": "completed",
  "items": [
    {
      "input_row_id": 1,
      "input_text": "Подложный бетон C25/30 23.57 m3",
      "urs_code": "801321111",
      "urs_name": "Beton podkladní C 16/20 až C 25/30",
      "unit": "m3",
      "quantity": 23.57,
      "confidence": 0.94,
      "extra_generated": false
    },
    {
      "input_row_id": 1,
      "urs_code": "801171321",
      "urs_name": "Bednění vodorovných konstrukcí",
      "unit": "m2",
      "quantity": 36.5,
      "confidence": 0.88,
      "extra_generated": true,
      "source": {
        "reason": "Сопутствующая работа к betonáži desky",
        "rule": "tech_rule_bedneni_for_slab"
      }
    }
  ],
  "warnings": [],
  "processing_time_ms": 4520
}
```

### Пример 3: Обработать одну строку вручную

```bash
curl -X POST http://localhost:3001/api/jobs/text-match \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Podkladní beton C25/30 tl. 100 mm",
    "quantity": 25,
    "unit": "m3"
  }'
```

**Ответ (200 OK):**
```json
{
  "candidates": [
    {
      "urs_code": "801321111",
      "urs_name": "Beton podkladní C 16/20 až C 25/30",
      "confidence": 0.94
    }
  ],
  "related_items": [
    {
      "urs_code": "801171321",
      "urs_name": "Bednění vodorovných konstrukcí",
      "reason": "tech_rule_bedneni_for_slab"
    }
  ]
}
```

---

## 📅 Фазы разработки

### Фаза 1: MVP-1 (2–3 недели)

Цель: базовый функционал без AI

**Deliverables:**
- Express.js сервер с REST API
- Парсер Excel/ODS/CSV
- Локальная БД с каталогом ÚRS (минимум 100 позиций для тестирования)
- Simple string-matching без AI
- JSON-ответы по формату из ТЗ
- 60%+ покрытие unit-тестами
- README с инструкциями

**Тесты приёма:**
- Загрузить 5 разных Excel-файлов → корректный парс
- Ручное введение 10 строк → матчинг работает
- API возвращает JSON по спеку

---

### Фаза 2: MVP-2 (2–3 недели)

Цель: LLM-интеграция + tech-rules

**Adds:**
- OpenAI/Claude для semantic matching
- Вычисление `confidence` по правилам
- 5–10 tech-rules (bednění, výztuž, přesun hmot, výkopy и т.д.)
- Экспорт в .xlsx
- Объяснения (почему выбран каждый код)

**Тесты приёма:**
- LLM возвращает правильные кандидаты
- Confidence вычисляется, пороги work корректно
- Tech-rules добавляют релевантные позиции
- Экспорт в Excel открывается

---

### Фаза 3: MVP-3 (2–3 недели)

Цель: Perplexity + веб-UI

**Adds:**
- Perplexity интеграция для поиска по ÚRS
- Веб-интерфейс (React / Vue)
- История jobs в БД
- Ручное редактирование результатов

---

### Фаза 4: Production (4+ недели)

Цель: масштабирование, надёжность, мониторинг

**Adds:**
- Очереди задач (Bull / RabbitMQ)
- Авторизация пользователей
- Кэширование результатов
- Мониторинг (Sentry / LogRocket)
- CI/CD (GitHub Actions / GitLab CI)

---

## 🔗 Ссылки и ресурсы

- **ÚRS каталог:** https://podminky.urs.cz/
- **OpenAI API docs:** https://platform.openai.com/docs/
- **Anthropic Claude:** https://docs.anthropic.com/
- **Perplexity API:** https://docs.perplexity.ai/
- **Express.js:** https://expressjs.com/
- **Jest (testing):** https://jestjs.io/

---

## ❓ Часто задаваемые вопросы

### Q: Можно ли начать разработку с другого стека (Python/Go)?

**A:** Да, но рекомендуется Node.js + Express для MVP-1 (быстрее). Позже можно переписать на другой язык, если нужно масштабирование.

### Q: Как получить данные ÚRS для локальной БД?

**A:** Нужно:
1. Выгрузить каталог с сайта ÚRS (проверить лицензию / права)
2. Или использовать Perplexity/web-scraping для заполнения БД (с уважением к robots.txt)
3. Для MVP-1 можно создать фиксированный набор из 100–200 позиций вручную

### Q: Когда подключать LLM?

**A:** На MVP-2. На MVP-1 используй простой string-matching. Это позволит понять требования и архитектуру перед добавлением сложности.

### Q: Как избежать генерирования несуществующих кодов ÚRS?

**A:** Жёсткое правило в коде LLM:
```
"Ты выбираешь КОД только из переданного списка.
Если подходящего кода нет, верни пустой результат.
НИКОГДА не придумывай новые коды, даже если они звучат логично."
```

### Q: Нужна ли авторизация для MVP-1?

**A:** Нет. Сделай заглушку (любой запрос принимается). Авторизация на MVP-3 / Production.

---

## 📝 Контрольный список для разработчика

### Перед началом:
- [ ] Прочитано полное ТЗ (URS_MATCHER_TZ.md)
- [ ] Выбран стек технологий (Node.js/Express рекомендуется)
- [ ] Выбрана БД (PostgreSQL / SQLite для MVP-1)
- [ ] Понимаю 5 основных сценариев использования
- [ ] Понимаю структуру JSON-ответа (раздел 4 ТЗ)

### При разработке MVP-1:
- [ ] Инициализирован Express сервер
- [ ] Написан парсер Excel/ODS/CSV
- [ ] Создана схема БД и таблица `urs_items`
- [ ] Реализованы endpoints (загрузка, получение результатов)
- [ ] Написаны unit-тесты (60%+ покрытие)
- [ ] Написан README

### При разработке MVP-2:
- [ ] Подключена OpenAI / Claude API
- [ ] Работает semantic matching
- [ ] Вычисляется confidence
- [ ] Реализованы 5+ tech-rules
- [ ] Работает экспорт в .xlsx

### При разработке MVP-3:
- [ ] Интегрирована Perplexity
- [ ] Написан фронтенд (React / Vue)
- [ ] История jobs сохраняется и загружается
- [ ] Веб-UI позволяет редактировать результаты

---

## 🎁 Что находится в этом комплекте?

```
STAVAGENT/
├── URS_MATCHER_TZ.md                    ← Полное техническое задание
├── URS_MATCHER_DEVELOPER_PROMPT.md      ← Готовый промпт для AI / разработчика
└── URS_MATCHER_README.md                ← Этот файл (навигация)
```

**Статус:** ✅ Готово для передачи разработчику / команде

**Версия:** 1.0

**Дата:** Ноябрь 2025

---

## 🚀 Как использовать этот комплект?

### Для project manager:
1. Отправить разработчику все три файла
2. Попросить спроектировать архитектуру на основе ТЗ
3. Согласовать стек технологий
4. Спланировать спринты по MVP

### Для backend-разработчика:
1. Прочитать ТЗ целиком
2. Использовать промпт для AI (в Playground)
3. Или спроектировать архитектуру самостоятельно
4. Реализовать MVP-1 по графику

### Для AI-assistant (Claude / ChatGPT):
1. Скопировать текст из `URS_MATCHER_DEVELOPER_PROMPT.md`
2. Вставить как System Prompt
3. В User Message вставить текст из `URS_MATCHER_TZ.md`
4. Получить готовый код и архитектуру

---

**Готово к использованию! 🎉**

Для вопросов и уточнений — обратись к автору ТЗ или к team lead.
