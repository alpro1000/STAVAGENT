# nabidka.xlsm — Internal Audit (Structure Only)

**Per task §Q9:** This file is INTERNAL business document (BERGER's price proposal for the
SÚSPK D&B tender). Audit captures **structure only** — sheet names, column headers, row
labels, formula locations. **No numeric values** are extracted to outputs.

**Audit date:** 2026-05-06
**Audit method:** Python `openpyxl` (read-only, formulas mode)
**Source:** `inputs/docx/nabídka.xlsm`

---

## 1. Workbook Layout

| Property | Value |
|---|---|
| Sheets | 1 |
| Sheet name | `CENOVÁ NABÍDKA` |
| Used range | `A1:XFD83` (max_col=16384 indicates full-width Excel header artifact, real data ≤ N) |
| Effective columns | A–N (14) |
| Effective rows | 6–17 (header block) + 18–82 (item area, mostly empty in template) |
| VBA / macros | Workbook is `.xlsm` — VBA layer present but **not extracted** (out of audit scope) |

---

## 2. Merged-Cell Map (Layout Anchors)

| Range | Purpose (inferred) |
|---|---|
| `A8:C8`, `A9:C9`, `A10:C10`, `A11:C11` | Left-column block: title + investor + období + supplementary metadata |
| `D8:H8`, `D9:H9` | Right-column header span (project / object name) |
| `A13:H13` | Section divider above the položka table |
| `I13:J13` | "Aktuální" period grouping label |
| `K13:L13` | "Od počátku" period grouping |
| `M13:N13` | "Zbývá" period grouping |

The merged-cell pattern confirms a **3-period accounting layout**: aktuální (this period) /
od počátku (cumulative) / zbývá (remaining) — typical for SO-úrovňovou fakturaci dle
zjišťovacího protokolu.

---

## 3. Header Row Map (Row 14 — Column Definitions)

| Col | Label | Role |
|---|---|---|
| A | `Poř. č.` | row ordinal |
| D | `Popis položky` | item description |
| E | `M.j.` | unit (ks, kpl, m, m², m³, t, …) |
| F | `kpl` | quantity-overall (template flag) |
| G | `jednotková cena` | unit price |
| H | `celková cena bez DPH` | total without VAT |
| I | `počet dnů` | days (period-specific quantity) |
| J | `cena celkem (Kč)` | period total |
| K | `množství` | qty (od počátku) |
| L | `čerpáno (Kč)` | drawn (od počátku) |
| M | `množství` | qty (zbývá) |
| N | `čerpáno (Kč)` | drawn (zbývá) |

This is a **fakturační šablona** (period-billing template), not a master soupis prací — it
mirrors a single SO/zjišťovací protokol covering installments (DIO + PD + …).

---

## 4. Top-Level Metadata Rows (6–11)

| Row / Cell | Field | Captured value (label only) |
|---|---|---|
| `A6` | Document title | `Cenová nabídka` |
| `A8` | Field label | `Název stavby:` |
| `K8` | Field label | `ke zjišťovacímu protokolu č.` |
| `N8` | Period number | `1` |
| `A9` | Field label | `Název objektu:` |
| `D9` | Project value | `most ev. Číslo 2062-1` |
| `A10` | Field label | `Fakturované období:` |
| `A11` | (empty padding row) | — |

Confirmed: this template is **specific to Most ev.č. 2062-1** (not a blank pre-tender form).

---

## 5. Item Area (Rows 15–17 — Visible Items)

| Row | Cell | Label | M.j. | Numeric cells (locations only) | Formula cells |
|---|---|---|---|---|---|
| 15 | `D15` | `zajištění DIO` | `ks` (E15) | F15, G15, H15, I15 | J15 (formula) |
| 16 | `D16` | `zpracování projektové dokumentace` | `M.j.` (E16) | A16, F16, G16, I16 | H16, J16 (formulas) |
| 17 | `D17` | `Celkem bez DPH` | — | — | J17, L17, N17 (totals formulas) |

The 2 visible položky (DIO + PD) are **business preliminaries** (DIO = Dopravně Inženýrské
Opatření / signing & traffic management; PD = Project Documentation). The item area below
row 17 is **template space** for ongoing fakturační doplňování.

---

## 6. Formula Inventory

| Cell | Length | Likely role (no extraction) |
|---|---|---|
| `XFD10` | 15 | Excel-internal placeholder (column XFD = column 16384) — likely auto-generated by macro |
| `J15` | 8 | Row 15 period total `=F*G` or similar |
| `H16` | 8 | Row 16 total bez DPH |
| `J16` | 8 | Row 16 period total |
| `J17` | 13 | Sum-formula across J column (`=SUM(...)` or similar) |
| `L17` | 11 | Sum-formula across L column |
| `N17` | 11 | Sum-formula across N column |
| `P57` | 12 | Likely subtotal in extended area (off-screen) |

No formulas span > 15 chars — confirms template has no complex multi-sheet logic.

---

## 7. Audit Conclusions

1. **Workbook is INTERNAL business artifact** — fakturační template, not master soupis.
   It mirrors the format used in SÚS Plzeňského kraje monthly billing protocol, populated
   per-period as the contract progresses.
2. **No double-counting risk vs. master soupis** — this is operational accounting, not
   tendr scope. It does NOT replace `04_documentation/soupis_praci_zihle_2062-1.xml`
   (master soupis built in Phase D).
3. **VBA layer not audited** — out of scope per Q9. If macros perform price calculations
   needed for cost reconciliation, a follow-up audit can be requested by the user.
4. **No numeric values captured** — output complies with internal-document policy:
   no jednotková cena, no celková cena, no množství values appear in this audit.

---

## 8. Cross-Reference Map

- `04_documentation/manual_reference_JS/SO_180_parsed.yaml` — user manual ground truth
  (Objízdná trasa) — different document, no overlap with `nabídka.xlsm`.
- `04_documentation/manual_reference_JS/SO_201_parsed.yaml` — user manual ground truth
  (Most ev.č. 20-005 Kfely template) — different document.
- `04_documentation/soupis_praci_zihle_2062-1.{xml,xlsx}` — Phase D master soupis (UNIXML
  + XLSX) — primary deliverable, separate from this fakturační template.
- `04_documentation/vendor_pricing/vendor_quotes.yaml` — public vendor quotes (allowed in
  outputs).

---

**End of structure-only audit. No price values transferred to repository outputs.**
