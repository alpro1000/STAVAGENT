# Архитектура интеграции URS Matcher ↔ STAVAGENT

**Дата:** 2025-11-24
**Статус:** Проектирование

---

## ❓ Вопрос: Где происходит обработка?

### ТЕКУЩАЯ АРХИТЕКТУРА (до интеграции):

```
┌─────────────────────────────────────────────────────────────┐
│ URS_MATCHER_SERVICE (standalone)                            │
│                                                              │
│  ┌──────────────┐                                           │
│  │ Frontend     │                                           │
│  │ - app.js     │                                           │
│  │ - File upload│                                           │
│  │   UI         │                                           │
│  └──────┬───────┘                                           │
│         │ POST /api/jobs/file-upload                        │
│         ▼                                                    │
│  ┌──────────────────────────────────────────┐               │
│  │ Backend (Express.js)                     │               │
│  │                                           │               │
│  │ ┌─────────────────────────────────────┐  │               │
│  │ │ routes/jobs.js                      │  │               │
│  │ │ - Multer file upload                │  │               │
│  │ │ - parseExcelFile()                  │  │               │
│  │ │ - matchUrsItems()                   │  │               │
│  │ │ - llmClient re-ranking              │  │               │
│  │ └─────────────────────────────────────┘  │               │
│  │                                           │               │
│  │ ┌─────────────────────────────────────┐  │               │
│  │ │ services/                           │  │               │
│  │ │ - fileParser.js (Excel/ODS)         │  │               │
│  │ │ - ursMatcher.js (URS search)        │  │               │
│  │ │ - llmClient.js (GPT-4)              │  │               │
│  │ │ - tridnikParser.js (TŘÍDNÍK)        │  │               │
│  │ └─────────────────────────────────────┘  │               │
│  │                                           │               │
│  │ ┌─────────────────────────────────────┐  │               │
│  │ │ SQLite Database                     │  │               │
│  │ │ - jobs (job metadata)               │  │               │
│  │ │ - job_items (results)               │  │               │
│  │ └─────────────────────────────────────┘  │               │
│  └───────────────────────────────────────────┘               │
│                                                              │
└──────────────────────────────────────────────────────────────┘

STAVAGENT (concrete-agent) - НЕ ИСПОЛЬЗУЕТСЯ СЕЙЧАС
```

**Ответ:** ✅ **ДА, у вас ЕСТЬ окно загрузки в URS_MATCHER_SERVICE**
         ❌ **НО обработка НЕ идет в ядро STAVAGENT (пока!)**

**СЕЙЧАС:** Все обрабатывается **внутри URS_MATCHER_SERVICE** (standalone).

---

## 🎯 ЦЕЛЕВАЯ АРХИТЕКТУРА (после интеграции):

### Вариант A: REST API интеграция (Рекомендуемый для Фазы 2+)

```
┌─────────────────────────────────────────────────────────────┐
│ URS_MATCHER_SERVICE (Frontend + Light Backend)              │
│                                                              │
│  ┌──────────────┐                                           │
│  │ Frontend     │                                           │
│  │ - Upload UI  │                                           │
│  │ - Results UI │                                           │
│  └──────┬───────┘                                           │
│         │ POST /api/jobs/file-upload                        │
│         ▼                                                    │
│  ┌───────────────────────────────┐                          │
│  │ Backend (API Gateway)         │                          │
│  │                               │                          │
│  │ Фаза 1:                       │                          │
│  │ ✅ Local processing           │                          │
│  │ - fileParser                  │                          │
│  │ - ursMatcher                  │                          │
│  │ - tridnikParser               │                          │
│  │                               │                          │
│  │ Фаза 2+:                      │                          │
│  │ ⬇️ Forward to STAVAGENT       │                          │
│  └──────────┬────────────────────┘                          │
└─────────────┼───────────────────────────────────────────────┘
              │ HTTP REST API
              │ POST /api/stavagent/parse-document
              │ POST /api/stavagent/extract-context
              │ POST /api/stavagent/multi-role-analyze
              ▼
┌─────────────────────────────────────────────────────────────┐
│ STAVAGENT (concrete-agent core)                             │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ REST API Endpoints (FastAPI/Express)                 │   │
│  │                                                       │   │
│  │ POST /api/parse-document                             │   │
│  │ → SmartParser → return parsed data                   │   │
│  │                                                       │   │
│  │ POST /api/extract-context                            │   │
│  │ → Document Q&A Flow → return project_context_json    │   │
│  │                                                       │   │
│  │ POST /api/multi-role-analyze                         │   │
│  │ → Multi-Role System → return expert analysis         │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ Core Services                                        │   │
│  │ - SmartParser (PDF/Excel/XML)                        │   │
│  │ - Document Q&A Flow                                  │   │
│  │ - Multi-Role System (6 AI roles)                     │   │
│  │ - TŘÍDNÍK classification                             │   │
│  │ - Knowledge Base (KROS, ČSN)                         │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ PostgreSQL Database                                  │   │
│  │ - projects                                           │   │
│  │ - documents                                          │   │
│  │ - qa_sessions                                        │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

**Преимущества:**
- ✅ Четкое разделение ответственности
- ✅ Независимое развитие сервисов
- ✅ Масштабируемость (можно запускать на разных серверах)
- ✅ Легко тестировать

**Недостатки:**
- ❌ Сетевые задержки (REST вызовы)
- ❌ Нужно разворачивать 2 сервиса
- ❌ Сложнее для локальной разработки

---

### Вариант B: Импорт модулей (Рекомендуемый для Фазы 1)

```
┌─────────────────────────────────────────────────────────────┐
│ URS_MATCHER_SERVICE (с импортом модулей STAVAGENT)          │
│                                                              │
│  ┌──────────────┐                                           │
│  │ Frontend     │                                           │
│  └──────┬───────┘                                           │
│         │                                                    │
│         ▼                                                    │
│  ┌─────────────────────────────────────────────┐            │
│  │ Backend                                     │            │
│  │                                             │            │
│  │ Фаза 1 (local):                            │            │
│  │ - fileParser.js                             │            │
│  │ - ursMatcher.js                             │            │
│  │ - tridnikParser.js                          │            │
│  │                                             │            │
│  │ Фаза 2+ (imported from STAVAGENT):         │            │
│  │ import { SmartParser } from                 │            │
│  │   '../../concrete-agent/.../smart_parser'  │            │
│  │                                             │            │
│  │ import { MultiRoleSystem } from             │            │
│  │   '../../concrete-agent/.../multi_role'    │            │
│  │                                             │            │
│  │ import { DocumentQA } from                  │            │
│  │   '../../concrete-agent/.../doc_qa'        │            │
│  └─────────────────────────────────────────────┘            │
│                                                              │
└──────────────────────────────────────────────────────────────┘
         │
         │ Shared codebase
         ▼
┌─────────────────────────────────────────────────────────────┐
│ STAVAGENT/concrete-agent (библиотека модулей)               │
│                                                              │
│  ├─ app/parsers/smart_parser.py                             │
│  ├─ app/services/multi_role.py                              │
│  └─ app/services/doc_qa.py                                  │
└──────────────────────────────────────────────────────────────┘
```

**Преимущества:**
- ✅ Нет сетевых задержек
- ✅ Проще для разработки (один процесс)
- ✅ Легко дебажить

**Недостатки:**
- ❌ Tight coupling (тесная связь)
- ❌ Сложнее версионирование (Python ↔ Node.js)
- ❌ Труднее масштабировать

---

## 📋 Рекомендуемая стратегия по фазам:

### **Фаза 1: Standalone (текущая архитектура)**
```
URS_MATCHER_SERVICE обрабатывает все сам:
- File upload → multer → local disk
- Parse → fileParser.js (Excel/ODS)
- Match → ursMatcher.js + llmClient.js
- Group → tridnikParser.js (NEW)
- Block analysis → Claude API direct (NEW)
- Results → SQLite → Frontend
```

**Интеграция с STAVAGENT:** ❌ НЕТ (пока не нужна)

---

### **Фаза 2: Hybrid (частичная интеграция)**
```
URS_MATCHER_SERVICE:
- File upload (как раньше)
- Basic parsing (fileParser.js для простых файлов)

Для сложных документов → вызов STAVAGENT:
- TechSpec.pdf → POST /stavagent/parse-document
- Extract context → POST /stavagent/extract-context
- Return → project_context_json
```

**Интеграция с STAVAGENT:** ✅ REST API (выборочно)

---

### **Фаза 3: Full Integration (полная интеграция)**
```
URS_MATCHER_SERVICE (Frontend + Thin API):
- Upload UI
- Results display
- Все тяжелые операции → STAVAGENT

STAVAGENT (Processing Core):
- Parse documents (SmartParser)
- Extract context (Document Q&A)
- Multi-Role analysis
- TŘÍDNÍK classification
- Tech_rules validation
```

**Интеграция с STAVAGENT:** ✅ REST API (везде)

---

## 🔧 Практическая реализация Фазы 1

### Что НЕ МЕНЯЕТСЯ:
```javascript
// routes/jobs.js (EXISTING)
router.post('/file-upload', upload.single('file'), async (req, res) => {
  // ✅ Остается как есть:
  const rows = await parseExcelFile(filePath); // Наш парсер
  const matches = await matchUrsItems(...);     // Наш поиск
  const usedLLM = await matchUrsItemWithAI(...); // Наш LLM
});
```

### Что ДОБАВЛЯЕТСЯ (NEW):
```javascript
// routes/jobs.js (NEW endpoint)
router.post('/block-match', upload.single('file'), async (req, res) => {
  // 1. Parse file (same as before)
  const rows = await parseExcelFile(filePath);

  // 2. Get project context (manual input for Phase 1)
  const projectContext = req.body.project_context; // От пользователя

  // 3. Group by TŘÍDNÍK (NEW)
  const { groupItemsByWorkType } = await import('../../services/tridnikParser.js');
  const grouped = groupItemsByWorkType(rows);

  // 4. For each block → block analysis (NEW)
  const blockResults = [];
  for (const [blockName, blockRows] of Object.entries(grouped)) {
    // Search URS candidates for all rows in block
    const candidates = await Promise.all(
      blockRows.map(row => matchUrsItems(row.description, row.quantity, row.unit))
    );

    // Call Claude with mode: "boq_block_analysis"
    const blockAnalysis = await analyzeBlock({
      mode: "boq_block_analysis",
      project_context: projectContext,
      boq_block: {
        block_id: blockName,
        rows: blockRows
      },
      urs_candidates: candidates
    });

    blockResults.push(blockAnalysis);
  }

  res.json({ blocks: blockResults });
});
```

---

## 🚀 Следующие шаги (Фаза 1):

1. ✅ **ROADMAP.md создан** - план видимости есть
2. ✅ **Архитектура уточнена** - понимаем как интегрироваться
3. ⏭️ **Создать `/api/jobs/block-match` endpoint**
4. ⏭️ **Расширить `ursMatcher.prompt.js` с mode: "boq_block_analysis"**
5. ⏭️ **Добавить Frontend UI для project_context input**
6. ⏭️ **Тестирование на реальном блоке**

---

## 💡 Ответ на ваш вопрос:

### "У НАС ЖЕ ЕСТЬ ОКНО ЗАГРУЗКИ И ПОТОМ ОБРАБАТЫВАЕТСЯ В ЯДРЕ?"

**СЕЙЧАС (Фаза 0):**
- ✅ **ДА**, есть окно загрузки в `URS_MATCHER_SERVICE/frontend`
- ❌ **НЕТ**, обработка **НЕ** идет в ядро STAVAGENT
- ℹ️ Все обрабатывается **внутри** `URS_MATCHER_SERVICE/backend`

**ПОСЛЕ Фазы 2-3:**
- ✅ **ДА**, окно загрузки останется в `URS_MATCHER_SERVICE/frontend`
- ✅ **ДА**, для **сложных документов** обработка **БУДЕТ** идти в ядро STAVAGENT
- ℹ️ Простые файлы (Excel BOQ) будут обрабатываться локально, сложные (PDF TechSpec) → STAVAGENT

---

## 📊 Сравнение подходов:

| Аспект | Фаза 1 (Standalone) | Фаза 2+ (Integrated) |
|--------|---------------------|----------------------|
| **Upload** | URS Matcher | URS Matcher |
| **Parse Excel** | fileParser.js (local) | fileParser.js (local) |
| **Parse PDF** | ❌ Не поддерживается | ✅ STAVAGENT SmartParser |
| **Extract Context** | ❌ Ручной ввод | ✅ STAVAGENT Document Q&A |
| **TŘÍDNÍK Group** | ✅ tridnikParser.js (local) | ✅ STAVAGENT enhanced |
| **Block Analysis** | ✅ Claude direct | ✅ STAVAGENT Multi-Role |
| **Tech Rules** | ❌ Нет | ✅ STAVAGENT tech_rules.js |
| **Database** | SQLite (local) | PostgreSQL (shared?) |

---

**Вывод:** Для Фазы 1 мы **НЕ ТРОГАЕМ** существующую загрузку и парсинг, просто **ДОБАВЛЯЕМ** новый endpoint `/block-match` с расширенной логикой.

Интеграция со STAVAGENT начнется в Фазе 2 (парсинг PDF/документов).

---

**Последнее обновление:** 2025-11-24
**Статус:** ✅ Архитектура определена - готовы к Фазе 1
