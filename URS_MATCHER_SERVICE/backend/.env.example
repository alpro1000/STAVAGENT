NODE_ENV=development
PORT=3001
DATABASE_URL=file:./data/urs_matcher.db
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,*

# ==========================================
# LLM Configuration (MVP-2: Claude/OpenAI)
# ==========================================
# Provider: 'claude' or 'openai'
LLM_PROVIDER=openai

# API Key for selected provider
# - For OpenAI: starts with 'sk-'
# - For Claude (Anthropic): starts with 'sk-ant-'
LLM_API_KEY=sk-...

# Model name (e.g., 'gpt-4', 'gpt-3.5-turbo' for OpenAI; 'claude-3-sonnet-20240229' for Claude)
LLM_MODEL=gpt-4

# LLM request timeout in milliseconds (default: 30000)
LLM_TIMEOUT_MS=30000

# Legacy support (optional, if using OpenAI)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4

# ==========================================
# URS Catalog Mode Configuration
# ==========================================
# Mode: 'local' (default) | 'perplexity_only' | 'local+perplexity' (future)
# - 'local': Use local SQLite database of URS codes
# - 'perplexity_only': Search via Perplexity API only (no local DB required)
# - 'local+perplexity': Try local first, fall back to Perplexity (future)
URS_CATALOG_MODE=perplexity_only

# ==========================================
# Perplexity API Configuration
# ==========================================
# API Key for Perplexity AI (for searching podminky.urs.cz)
# Get from: https://www.perplexity.ai/
PPLX_API_KEY=pplx-...

# Perplexity model to use (default: sonar for web search)
PPLX_MODEL=sonar

# Perplexity request timeout in milliseconds (default: 30000)
PPLX_TIMEOUT_MS=30000
