# TASKS — индекс/бэклог STAVAGENT (2026-06-19)

> Полная очередь заданий из статус-дока. Каждое — handoff-готовое.
> **Источник состояния:** `docs/handoff/STAVAGENT_STATUS_HANDOFF_2026-06-19.md`.
> **Формат работы:** агент читает CLAUDE.md → STATUS → таск; recon-таски доходят до гейта и СТОП; имплементация — после твоего ревью; по завершении агент **обновляет STATUS** (чекбоксы + находки).
>
> 📄 = есть отдельный файл-спека · 📝 = компактная спека здесь (разверну в файл, когда возьмёшь в работу)

---

## 🔴 PRE-CEMEX (до 28.06 — demo, не идеал)

### T1 📄 — Локализация общестроительного атомизатора
`TASK_atomizer-obshchestroy-localize_2026-06-19.md`. Recon: найти тест-проверенную разбивку работ всех типов, понять «та же / параллельная» относительно MCP-атомизатора, спланировать порт шаблонов декомпозиции на ÚRS-биндинг. **Гейт перед кодом.**
**Зависимости:** нет. **Кормит:** T7 (свод киосков).

### T2 📄 — Chunk-извлечение → Kalkulátor
`TASK_chunk-extract-to-kalkulator_2026-06-19.md`. Recon: найти chunk-логику Analýzy dokumentace, диагностировать на чём падала, спланировать перенос «документ→элементы→вход калькулятора» (передняя половина шва). **Гейт перед кодом.**
**Зависимости:** нет. **Кормит:** T7 (можно прятать киоск только после спасения логики), V1/SmartInput (диагноз падения).

### T3 📝 — Fix 3: версия каталога + ранжирование
**Контекст:** `source` захардкожен `"OTSKP 1/2025"` на всех путях (подтверждено живьём); при равном confidence наверх всплывает дороже-предпрягнутый код (демо-регрессия 334335 > 334325).
**Scope:** (A) убрать строковый хардкод → штамповать реальный catalog_version отдавшей записи; (B) keyword-кандидатный SQL — сортировку по цене заменить на relevance/score, проверить тай-брейк в детерминированном ранкере (не должен ломать тай по цене).
**Гейт:** golden-кейс `C30/37 без предпряга → 334325 первым` зелёный.
**Done:** хардкод убран; демо-кейс отдаёт železobeton первым; источник версии — из данных. **Бетон-цены не трогать.**
**Зависимости:** перед T6-rebake обязателен (иначе ребейк не проверить). Демо-релевантно.

### T4 📝 — Fix 4: диагностика раскола (read-only SQL)
**Контекст:** keyword 2025/17904 vs embeddings 2026/17940; точные числа с MCP не достать.
**Scope:** SQL-замеры — версия pgvector (`SELECT extversion …`); count строк keyword-store vs embeddings-store + их version-поле; подтвердить дельту 36 и характер раскола.
**Гейт:** только чтение, без изменений.
**Done:** числа и версии зафиксированы в STATUS §3; решение «связать rebake с миграцией Phase 3» подтверждено или отклонено фактами.
**Зависимости:** нет. **Кормит:** T6.

### T5 📝 — Phase 2 MCP: find_urs_code
**Контекст:** URS-инструмент без версионного штампа; счётчики каталога надо привести к 17940; docstring без живого примера.
**Scope:** carrier-shape для find_urs_code (единый контракт ответа с OTSKP-стороной, где уместно); счётчики → 17940; docstring-example.
**Гейт:** контракт ответа не ломает существующих потребителей (Portal/URS/Registry/Monolit).
**Done:** ответ единообразен; счётчики верны; пример в docstring.
**Зависимости:** мягко связан с T3 (единый стиль провенанса источника).

---

## 🟡 POST-CEMEX (после 28.06)

### T6 📝 — Векторная миграция 3072 / halfvec
**Scope:** исполнение фазового плана STATUS §4 (gemini-embedding-001, 3072, halfvec, без обрезки). Фазы 0–2 безопасны хоть сейчас (проверки + аддитивная колонка + код за флагом); Фазы 3+ — после Cemex.
**Развилки:** связать rebake Fix 4 в Фазу 3 (один re-embed = 2026); делать Krok A (chat) ради общего genai-клиента.
**Гейт:** после Фазы 4 — рекалибровка порогов под 3072-пространство до cutover.
**Done:** doc+query в 3072-пространстве, golden зелёный, откат через флаг возможен.
**Зависимости:** T3 (честная версия), T4 (числа), частично Krok A.

### T7 📝 — Чистка киосков фронта
**Scope:** спрятать Analýza dokumentace (BETA) — **только после T2**; убрать Multi-Role тег/окно; активировать Generátor výkazu výměr (бетон-часть рабочая); свести URS-matcher + Generátor на единый MCP-атомизатор — **по плану из T1**.
**Гейт:** ничего не удалять физически — только не показывать; параллельные реализации сводить, не плодить.
**Done:** дашборд показывает только рабочее; нет дублей механизма.
**Зависимости:** T1 (свод), T2 (спасение chunk-логики до скрытия).

### T8 📝 — Рассинхрон доков
**Scope:** CLAUDE.md «9 tools» → 20, «4 roles» → 5; лендинг «6 roles» → 5.
**Done:** числа сходятся с кодом (`EXPECTED_TOOLS`, Role enum).
**Зависимости:** нет. Дешёвый, можно фоном.

### T9 📝 — URS Perplexity-ветка
**Контекст:** отдаёт деградированный «пришлите каталог» (conf 0.5), спасает matcher service.
**Scope:** усилить промпт/фоллбек ИЛИ осознанно оставить под прикрытием matcher и понизить вес.
**Гейт:** решение «чинить vs парковать» — за тобой.
**Зависимости:** нет.

---

## 🔵 VISION / БЭКЛОГ (зависит от решений по шву)

### V1 📝 — «Шов»: ТЗ → извлечение → KB → ИИ предлагает → движок считает
Куски: SmartInput PDF pipeline (использует диагноз чанков из T2); `tz_facts` напойка (технология → planElement + MCP); AI advisor v2 (цитирование норм с live-валидацией); **Multi-Role оркестратор = шаг «ИИ предлагает/обосновывает»** (припаркован сюда). Не проект с нуля — достройка моста между готовыми частями.

### V2 📝 — Pattern 27: внешняя LLM как N-й слой кросс-валидации матчинга
Независимая модель перепроверяет каталожный матчинг. Не построен.

---

## ⚙️ РЕШЕНИЯ / ИНФРА (не код-таски, держать на радаре)

- **D1 — Cloud SQL ZONAL → HA?** Вопрос в рамках кредитов (Google call Q6).
- **D2 — `min-instances=0` cold-start** теряет in-memory KB cache — баланс цена/латентность.
- ⚠️ **Runtime 5 ролей** — проверяется только вызовом с твоей машины / через фронт (egress отсюда закрыт).

---

## Порядок, если коротко

1. Параллельно безопасное: **T4** (read-only SQL), **T8** (доки), **T1/T2** (recon до гейта).
2. Демо-критичное до 28.06: **T3**, затем **T5**.
3. После Cemex: **T6** (миграция, rebake внутри), **T7** (чистка по findings T1/T2), **T9**.
4. Затем шов: **V1** (Multi-Role оживает здесь), **V2**.
