# Geometry Calculator — deployment status audit

**Date:** 2026-05-14
**Driver:** spec 24.03.2026 (chat-only — not committed to repo)
**Scope:** read-only inventory; no files modified.

## Status per spec'd file

| File | Status | Evidence |
|------|--------|----------|
| `GeometryCalculator.jsx` | ❌ Never deployed | `find` returns nothing; `git log --diff-filter=A` empty for the path. |
| `GeometryModal.jsx` + `SubElementInput.jsx` + `ShapeSelector.jsx` + `ResultsSummary.jsx` + `AINormAdvisor.jsx` | ❌ Never deployed | Same — no file anywhere in `concrete-agent/frontend`, `stavagent-portal/frontend`, `Monolit-Planner/frontend`, `URS_MATCHER_SERVICE`, `rozpocet-registry`. |
| `app/services/geometry_calculator.py` | ❌ Never deployed | `find ... -name "geometry_calculator.py"` empty. |
| `app/api/routes_geometry.py` | ❌ Never deployed | Same — and `grep -r "routes_geometry"` returns 0 matches. |
| `/api/geometry/catalog`, `/api/geometry/calculate`, `/api/geometry/analyze-image` | ❌ Never registered | `grep -r "api/geometry"` across `.py`/`.tsx`/`.jsx`/`.ts` returns 0 matches. |
| "📐 Geometrie" button in `PositionTable.jsx` | ❌ Never added | Only "geometrie" hits in `stavagent-portal/frontend/src/pages/LandingPage.tsx:191,222` (marketing copy, not a UI button). |

**Spec docs:** no `*.md` references `GeometryCalculator` or `GeometryModal`. The 24.03.2026 spec lives only in chat history; nothing was ever committed.

## Context (why it never shipped)

1. **ADR-005** (`docs/architecture/decisions/ADR-005_phase_E_dropped.md`, 2026-05-07) formally dropped engineering geometry from product scope: *"engineering drawings stay with autorizovaný projektant (ČKAIT signature, zákon 360/1992 Sb.); STAVAGENT moat is text/data layer, geometry extraction kept only as validation tool."* This was the Žihle 2062-1 pilot outcome.
2. **What does exist instead** (different scope, do not confuse): (a) `Monolit-Planner/shared/src/classifiers/element-classifier.ts:estimateExpectedVolume()` — sanity check from v4.22 ("volume-vs-geometry") flags ratios outside 0,3–3×; (b) "Geometrie" section *inside* the existing concrete calculator form (`CalculatorFormFields.tsx` D5–D8 L×W×H block for foundation types); (c) `test-data/hk212_hala/outputs/phase_0b_rerun/section_3_2_facts_geometry.md` — extracted-facts artifact, unrelated to a UI calculator.

## Verdict

All six spec'd files: **❌ Never deployed (spec only).** No partial implementation exists. The spec was superseded by ADR-005 — no further work required unless the product positioning around engineering geometry is revisited.
