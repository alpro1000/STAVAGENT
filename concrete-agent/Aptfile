# System dependencies for MinerU 2.x (pip: mineru[all])
# Cloud Build / apt-get installs these for OCR fallback

# Poppler - PDF rendering library
poppler-utils

# Tesseract - OCR engine
tesseract-ocr
tesseract-ocr-ces

# Image processing
libmagic1
